# Environment Variables
NODE_ENV=development
PORT=3000

# GitHub API (Optional - for higher rate limits)
# GITHUB_TOKEN=your_github_personal_access_token

# ==========================================
# Email Configuration
# ==========================================
#
# Two modes are supported:
# - smtp: Real mailbox (Google Workspace/Microsoft 365/Zoho) - sends FROM your domain
# - forward_only: Uses your personal email as From (fallback mode)

# Required Variables
EMAIL_MODE=smtp                              # 'smtp' or 'forward_only'
EMAIL_HOST=smtp.gmail.com                    # SMTP server host
EMAIL_PORT=587                               # SMTP port (587 for TLS, 465 for SSL)
EMAIL_USER=your-email@gmail.com              # SMTP username (your email)
EMAIL_PASS=your-app-password                 # SMTP password (use App Password for Gmail)
EMAIL_TO=manaskumarbehera1@outlook.com       # Where contact form notifications go

# Optional Variables (have sensible defaults)
# EMAIL_FROM=web@manaskumarbehera.com        # From address (default: EMAIL_USER for forward_only, EMAIL_REPLY_TO_DOMAIN for smtp)
# EMAIL_FROM_NAME=Manas                      # Display name in From header
# EMAIL_SECURE=false                         # TLS (auto: true if port 465)
# EMAIL_REPLY_TO_DOMAIN=web@manaskumarbehera.com  # Reply-To for forward_only mode

# ==========================================
# Provider-Specific Examples
# ==========================================

# Gmail (with App Password)
# EMAIL_MODE=smtp
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-gmail@gmail.com
# EMAIL_PASS=xxxx-xxxx-xxxx-xxxx    # App Password (16 chars, no spaces)
# EMAIL_TO=your-gmail@gmail.com

# Google Workspace (custom domain)
# EMAIL_MODE=smtp
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=web@manaskumarbehera.com
# EMAIL_PASS=xxxx-xxxx-xxxx-xxxx    # App Password
# EMAIL_TO=manaskumarbehera1@outlook.com
# EMAIL_FROM=web@manaskumarbehera.com

# Outlook/Microsoft 365
# EMAIL_MODE=smtp
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@outlook.com
# EMAIL_PASS=your-password
# EMAIL_TO=your-email@outlook.com

# Zoho Mail
# EMAIL_MODE=smtp
# EMAIL_HOST=smtp.zoho.com
# EMAIL_PORT=587
# EMAIL_USER=web@manaskumarbehera.com
# EMAIL_PASS=your-zoho-password
# EMAIL_TO=manaskumarbehera1@outlook.com
# EMAIL_FROM=web@manaskumarbehera.com

# Forward-Only Mode (uses personal email as sender)
# EMAIL_MODE=forward_only
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-gmail@gmail.com
# EMAIL_PASS=xxxx-xxxx-xxxx-xxxx
# EMAIL_TO=your-gmail@gmail.com
# EMAIL_REPLY_TO_DOMAIN=web@manaskumarbehera.com

# ==========================================
# Other Configuration
# ==========================================

# Admin Key for recommendation approval
# ADMIN_KEY=your-secret-admin-key

# Astratis Analytics
# ASTRATIS_API_KEY=your-astratis-api-key
